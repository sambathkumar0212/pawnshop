# Django Settings
DEBUG=True
SECRET_KEY=RtHTKGfBuohdP1HcFSLYjzjjIyRRKgcnrBa1ry8WvpY  # Replace with a key generated by: python -c "import secrets; print(secrets.token_urlsafe())"
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# SQLite (default)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# PostgreSQL (recommended for production)
DATABASE_ENGINE=django.db.backends.postgresql
DATABASE_NAME=pawnshop
DATABASE_USER=pawnshop_admin
DATABASE_PASSWORD=secure_password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com          # For Gmail; update if using another provider
EMAIL_PORT=587                     # Typically 587 for TLS; use 465 for SSL
EMAIL_USE_TLS=True                 # Set to False if using SSL (then use EMAIL_USE_SSL=True)
EMAIL_HOST_USER=sambath0212@gmail.com   # Replace with your email address
EMAIL_HOST_PASSWORD=Sambath@2r    # Use an app-specific password if needed
DEFAULT_FROM_EMAIL=Pawnshop Management <sambath0212@gmail.com>

# Face Recognition Settings
FACE_RECOGNITION_MODEL=hog  # 'hog' is faster but less accurate, 'cnn' requires GPU but is more accurate
FACE_RECOGNITION_TOLERANCE=0.6  # Lower is stricter

# Security Settings
SECURE_SSL_REDIRECT=False  # Set to True in production
SESSION_COOKIE_SECURE=False  # Set to True in production
CSRF_COOKIE_SECURE=False  # Set to True in production